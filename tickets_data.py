# tickets_data.py

TICKETS_DATA = [
    {
        "id": 1,
        "title": "Уровни описания структур данных.",
        "theory": "1. <b>Логический уровень</b>: описывается логическая структура объектов и их функциональное поведение. Определяет, какие операции могут быть выполнены над объектами.\n2. <b>Физический уровень</b>: рассматривается физическое представление объектов и способы их хранения (сплошное и цепное).\n3. <b>Аппаратный уровень</b>: рассматривается аппаратная поддержка структур данных.",
        "code": None,
        "keywords": "Логический, Физический, Аппаратный, сплошное, цепное",
        "code_error_desc": None
    },
    {
        "id": 2,
        "title": "Статические и динамические объекты программ.",
        "theory": "<b>Статические характеристики</b> остаются неизменными во время исполнения области действия объекта (типы, размеры массивов). Определяются по тексту программы.\n<b>Динамические характеристики</b> изменяются в процессе выполнения программы (значения переменных, связи с другими объектами). Определяются только во время выполнения.",
        "code": None,
        "keywords": "Статические, Динамические, по тексту программы",
        "code_error_desc": None
    },
    {
        "id": 3,
        "title": "Ссылочный тип данных.",
        "theory": "Ссылочный тип данных представляет множество значений, которые указывают на объекты определенного типа. Используется для работы с <b>динамическими объектами</b>.\nОперация <b>разыменования</b> используется для получения доступа к значению объекта.\nОсновная проблема — возможность утери ссылок, что приводит к <b>утечкам памяти</b>.",
        "code": None,
        "keywords": "разыменования, динамическими объектами, утечкам памяти",
        "code_error_desc": None
    },
    {
        "id": 4,
        "title": "Файл. Функциональная спецификация.",
        "theory": "Файлы являются <b>динамическими объектами</b>, размещенными во внешней памяти ЭВМ.\nФункциональная спецификация включает:\n- Множество значений (последовательности компонент).\n- Базовое множество атрибутов: операция <b>конкатенации</b> (слияния), присваивания, равенства.\n- Функции: создание пустого файла, доступ к компонентам, модификация, уничтожение.",
        "code": None,
        "keywords": "динамическими объектами, конкатенации, уничтожение",
        "code_error_desc": None
    },
    {
        "id": 5,
        "title": "Файл. Логическое описание. Физическое представление.",
        "theory": "Логическое описание позволяет работать с внешней памятью.\nВ Паскале используется именованный файловый тип и <b>буфер файла</b>. Операции: <b>reset</b> (подготовка к чтению), <b>rewrite</b> (режим записи).\nВ Си используется тип <b>FILE*</b> и функции стандартной библиотеки stdio.h.\nФизическое представление связано с внешними устройствами (диски, принтеры).",
        "code": None,
        "keywords": "FILE*, буфер файла, reset, rewrite",
        "code_error_desc": None
    },
    {
        "id": 6,
        "title": "Вектор. Функциональная спецификация. Логическое описание и физическое представление.",
        "theory": "Векторы (динамические массивы) размещаются в <b>куче</b> и позволяют задавать размер произвольно.\nФункциональная спецификация обеспечивает <b>постоянное время доступа</b> к элементам.\nВ Си для изменения размера используется <b>realloc</b>, для освобождения памяти — <b>free</b>.",
        "code": "#include <stdbool.h>\n\nbool Empty(Vector* v) { return v->size == 0; }\nint Size(Vector* v) { return v->size; }\n\nT Load(Vector* v, int i) {\n    if ((i >= 0) && (i < v->size)) return v->data[i];\n}\n\nvoid Save(Vector* v, int i, T t) {\n    if ((i >= 0) && (i < v->size)) v->data[i] = t;\n}\n\nvoid Resize(Vector* v, int sz) {\n    v->size = sz;\n    v->data = realloc(v->data, sizeof(T) * v->size);\n}",
        "keywords": "куче, realloc, free, O(1)",
        "code_error_desc": "В функциях Load и Save отсутствует возврат значения или обработка ошибки, если индекс выходит за границы."
    },
    {
        "id": 7,
        "title": "Очередь. Функциональная спецификация.",
        "theory": "Очередь — структура с одной читающей головкой на одном конце и одной записывающей на другой. Принцип <b>FIFO</b> (первым пришел - первым ушел).\nОперации: create (создать), is_empty (проверить на пустоту), push_back (добавить в конец), pop_front (извлечь первый), front (первый элемент), size (длина), destroy (уничтожить).",
        "code": None,
        "keywords": "FIFO, push_back, pop_front",
        "code_error_desc": None
    },
    {
        "id": 8,
        "title": "Очередь. Логическое описание и физическое представление(файл).",
        "theory": "Реализация на <b>кольцевом буфере</b> позволяет ограничить потенциально большой файл длиной capacity. Это дает возможность удалять элемент за <b>O(1)</b>, просто передвигая голову очереди, без копирования файла.",
        "code": "typedef struct {\n    FILE* buf;\n    int size;\n    int cap;\n    int head;\n} queue;\n\nvoid init(queue* q, char* argv) {\n    FILE* buf = fopen(argv, \"rw+\");\n    q->size = 0; q->buf = buf; q->head = 0; q->cap = POOL_SIZE;\n}\n\nbool push_back(queue* q, int val) {\n    if (q->size >= q->cap) return false;\n    fseek(q->buf, ((q->head + q->size) % q->cap) * sizeof(int), SEEK_SET);\n    fwrite(&val, sizeof(int), 1, q->buf);\n    q->size++; return true;\n}",
        "keywords": "кольцевом буфере, O(1), fseek, fwrite",
        "code_error_desc": "В функции push_back используется fseek и fwrite, но не проверяется успешность их выполнения."
    },
    {
        "id": 9,
        "title": "Очередь. Логическое описание и физическое представление(массив).",
        "theory": "3 стратегии отображения очереди на массиве:\n1. <b>Трудоголик</b>: голова фиксирована, хвост подвижен. Удаление за O(n).\n2. <b>Ленивая</b>: голова и хвост подвижны.\n3. <b>Кольцевой буфер</b>: соединение конца с началом через остаток от деления. Добавление и извлечение за <b>O(1)</b>. Не образуется мусор.",
        "code": "#define QU_CAP 100\n\ntypedef struct {\n    double buff[QU_CAP];\n    int size;\n    int head;\n} queue;\n\nbool push_back(queue* q, double val) {\n    if (q->size >= QU_CAP) return false;\n    q->buff[(q->size + q->head) % QU_CAP] = val;\n    q->size++; return true;\n}\n\ndouble pop_front(queue* q) {\n    double res = q->buff[q->head];\n    q->head = (q->head + 1) % QU_CAP;\n    q->size--; return res;\n}",
        "keywords": "Кольцевой буфер, остатка от деления, O(1)",
        "code_error_desc": "В pop_front не проверяется, пуста ли очередь (size == 0), что может привести к некорректному поведению."
    },
    {
        "id": 10,
        "title": "Очередь. Логическое описание и физическое представление(динамические объекты).",
        "theory": "Реализация через <b>связанные списки</b>. Нужна ссылка на начальный и конечный элементы, что обеспечивает добавление элементов в очередь за <b>O(1)</b>.",
        "code": "typedef struct node node;\nstruct node {\n    double val;\n    node* prev; \n};\ntypedef struct {\n    node* tail;\n    node* head;\n    int size;\n} queue;\n\nbool push_back(queue* q, double val) {\n    node* new_node = (node*)malloc(sizeof(node));\n    if (!new_node) return false;\n    new_node->val = val;\n    new_node->prev = q->tail;\n    q->tail = new_node;\n    q->size++; return true;\n}",
        "keywords": "связанные списки, malloc, O(1)",
        "code_error_desc": "В коде используется односвязный список (только prev), но для очереди обычно удобнее хранить next, чтобы движение было от head к tail. Также в pop_front нужно освобождать память (free)."
    },






    {
        "id": 11,
        "title": "Дек. Описание. Примеры задач.",
        "theory": "Дек (двусторонняя очередь) — структура данных, позволяющая добавлять и удалять элементы как с начала, так и с конца. Объединяет функциональность <b>стека</b> и <b>очереди</b>.\nОперации: push_back, pop_front, push_front, pop_back.\nПримеры задач: обход графа в ширину (<b>BFS</b>), реализация <b>LRU-кэша</b>, проверка сбалансированности скобок.",
        "code": "#define CAP 100\ntypedef struct {\n    double buff[CAP];\n    int size;\n    int head;\n    int tail;\n} deque;\n\nbool push_front(deque* dq, double val) {\n    if (dq->size >= CAP) return false;\n    dq->buff[(CAP + dq->head - 1) % CAP] = val;\n    dq->head = (CAP + dq->head - 1) % CAP;\n    dq->size++;\n    return true;\n}",
        "keywords": "двусторонняя очередь, стека, очереди, BFS, LRU-кэша",
        "code_error_desc": "В функции push_front при вычислении нового индекса головы нужно использовать конструкцию (CAP + head - 1) % CAP. Если просто написать (head - 1) % CAP, то при head=0 в языке Си получится отрицательное число, что сломает логику кольцевого буфера."
    },
    {
        "id": 12,
        "title": "Стек. Функциональная спецификация.",
        "theory": "Стек — структура данных, работающая по принципу <b>LIFO</b> (Last In, First Out — <b>последним пришел, первым ушел</b>). Чтение из стека разрушает его содержимое.\nОперации: create, is_empty, <b>push</b> (в стек), <b>pop</b> (из стека), <b>top</b> (вершина), destroy.\nШироко используется для синтаксического анализа, управления локальными переменными и рекурсии.",
        "code": None,
        "keywords": "LIFO, последним пришел, первым ушел, push, pop, top",
        "code_error_desc": None
    },
    {
        "id": 13,
        "title": "Стек. Логическое описание.",
        "theory": "Стек — <b>рекурсивная</b> структура данных. Может быть описан как <b>конкатенация</b> элемента типа T и стека S. Для эффективной вычислимости необходим <b>терминатор</b>, ограничивающий глубину рекурсии (его роль играет пустой стек).\nУдобно для вычисления выражений в <b>постфиксной</b> форме (<b>обратная польская запись</b>). Язык Форт использует стековую модель вычислений.",
        "code": None,
        "keywords": "рекурсивная, конкатенация, терминатор, постфиксной, обратная польская запись",
        "code_error_desc": None
    },
    {
        "id": 14,
        "title": "Стек. Физическое представление(массив).",
        "theory": "Реализация стека на статическом массиве фиксированной длины (<b>POOL_SIZE</b>). Переменная <b>size</b> служит счетчиком элементов и одновременно указывает на индекс <b>вершины</b> стека. Добавление (push) и удаление (pop) происходят за <b>O(1)</b>.",
        "code": "#define POOL_SIZE 100\ntypedef struct {\n    double buff[POOL_SIZE];\n    int size;\n} stack;\n\nbool push(stack* s, double val) {\n    if (s->size >= POOL_SIZE) return false;\n    s->buff[s->size] = val;\n    s->size++;\n    return true;\n}\n\ndouble pop(stack* s) {\n    s->size--;\n    double res = s->buff[s->size];\n    return res;\n}",
        "keywords": "POOL_SIZE, size, вершины, O(1)",
        "code_error_desc": "В функции pop не проверяется, пуст ли стек (size == 0). Если вызвать pop для пустого стека, size станет равным -1, и произойдет обращение к buff[-1], что вызовет неопределенное поведение или падение программы."
    },
    {
        "id": 15,
        "title": "Стек. Физическое представление(динамические объекты).",
        "theory": "Реализация стека на <b>односвязном списке</b>. Каждый узел содержит значение и указатель на предыдущий элемент (<b>prev</b>). Указатель <b>head</b> всегда ссылается на вершину стека. Добавление (push) и удаление (pop) происходят за <b>O(1)</b>, так как мы работаем только с головой списка.",
        "code": "typedef struct node node;\nstruct node {\n    double val;\n    node* prev;\n};\ntypedef struct {\n    node* head;\n    int size;\n} dyn_stack;\n\nbool s_push(dyn_stack* s, double val) {\n    node* new_node = (node*)malloc(sizeof(node));\n    if (!new_node) return false;\n    new_node->val = val;\n    new_node->prev = s->head;\n    s->head = new_node;\n    s->size++;\n    return true;\n}\n\ndouble s_pop(dyn_stack* s) {\n    node* old = s->head;\n    s->head = old->prev;\n    double val = old->val;\n    free(old);\n    s->size--;\n    return val;\n}",
        "keywords": "односвязном списке, prev, head, O(1), malloc, free",
        "code_error_desc": "В функции s_pop студенты часто забывают вызвать free(old) перед возвратом значения. Это приводит к классической утечке памяти, так как узел стека остается в куче, но ссылка на него теряется."
    },
    {
        "id": 16,
        "title": "Линейный список. Функциональная спецификация.",
        "theory": "Линейный список — структура данных, <b>обобщающая</b> стеки и очереди. В отличие от них, позволяет получить доступ к любому элементу без извлечения предыдущих. Упорядоченное множество элементов, где порядок определяется <b>относительным расположением</b>, а не индексами.\nОперации: <b>insert</b>, <b>delete</b>, first, last, next, prev.",
        "code": None,
        "keywords": "обобщающая, относительным расположением, insert, delete",
        "code_error_desc": None
    },
    {
        "id": 17,
        "title": "Линейный список. Логическое описание.",
        "theory": "Линейный список представляет собой упорядоченное множество элементов. <b>Двусвязные списки</b> являются частным случаем, где каждый элемент имеет ссылки на предыдущий и следующий. Чтобы <b>закольцевать</b> список, последний элемент указывает на первый, а первый — на последний.\nПрименяется для управления заданиями с <b>приоритетами</b>.",
        "code": None,
        "keywords": "Двусвязные списки, закольцевать, приоритетами",
        "code_error_desc": None
    },
    {
        "id": 18,
        "title": "Линейный список. Физическое представление. Итераторы.",
        "theory": "<b>Итераторы</b> — объекты, позволяющие удобно <b>навигироваться</b>, читать и записывать элементы списка. Они обеспечивают доступ к первому, последнему, предыдущему и следующему элементам.\nИспользование итераторов упрощает обход и работу с частями списка, скрывая внутреннюю структуру (массив это или динамические узлы).",
        "code": None,
        "keywords": "Итераторы, навигироваться, двусвязный список",
        "code_error_desc": None
    },
    {
        "id": 19,
        "title": "Линейный список. Физическое представление(массив).",
        "theory": "Реализация списка на статическом массиве с использованием управляющего индекса. Массив хранит элементы и \"свободные\" ячейки. Для быстрого удаления/вставки используется указатель <b>first_empty_ind</b> (или top), который связывает свободные ячейки в свой собственный <b>односвязный список</b> внутри массива. Используется <b>терминатор</b> для упрощения граничных условий.",
        "code": "#define POOL_SIZE 100\ntypedef struct node node;\nstruct node {\n    node* next;\n    node* prev;\n    int val;\n};\ntypedef struct {\n    node* head;\n    int size;\n    node* top; // первый свободный элемент\n    node buff[POOL_SIZE + 1]; // +1 для терминатора\n} List;\n\nvoid init(List* l) {\n    for (int i = 0; i < POOL_SIZE; ++i) {\n        l->buff[i].next = &(l->buff[i+1]);\n    }\n    l->buff[POOL_SIZE-1].next = NULL;\n    l->head = &(l->buff[POOL_SIZE]); // терминатор\n    l->head->prev = l->head->next = l->head;\n    l->top = &(l->buff[0]); \n    l->size = 0;\n}",
        "keywords": "first_empty_ind, односвязный список, терминатор",
        "code_error_desc": "При инициализации массива (в функции init) забывают корректно инициализировать терминатор. У терминатора (фиктивного элемента) оба указателя (prev и next) должны указывать на самого себя, иначе обход пустого списка зациклится или упадет."
    },
    {
        "id": 20,
        "title": "Линейный список. Физическое представление(динамические объекты).",
        "theory": "Реализация на <b>двунаправленном кольцевом списке</b>. Используется фиктивный элемент (<b>терминатор</b> или <b>голова-хвост</b>), next которого указывает на первый элемент, а prev — на последний. Это сводит количество указателей на список до одного и упрощает граничные условия (не нужно отдельно проверять пустоту для вставки в начало/конец).",
        "code": None,
        "keywords": "двунаправленном кольцевом списке, терминатор, голова-хвост",
        "code_error_desc": None
    },
    {
        "id": 21,
        "title": "Списки общего вида.",
        "theory": "Списки общего вида — сложные <b>нелинейные</b> структуры, обобщающие линейные списки. Элементами могут быть другие списки. Примеры: <b>ортосписки</b> (элемент ссылается на несколько других), <b>суперсписки</b>.\nЯркий пример применения — <b>разреженные матрицы</b>, где хранятся только ненулевые элементы и их координаты, что экономит огромные объемы памяти.",
        "code": None,
        "keywords": "нелинейные, ортосписки, суперсписки, разреженные матрицы",
        "code_error_desc": None
    },
    {
        "id": 22,
        "title": "Деревья. Двоичные деревья.",
        "theory": "Дерево — <b>нелинейная</b> структура, образованная <b>корнем</b> и конечным множеством поддеревьев. <b>Бинарное</b> (двоичное) дерево — дерево, в котором каждая вершина имеет не более двух дочерних узлов (<b>левое</b> и <b>правое</b> поддерево).\nПримеры: дерево арифметических выражений, семейное дерево, файловая система.",
        "code": "struct node {\n    int val;\n    node* left;\n    node* right;\n};",
        "keywords": "нелинейная, корнем, Бинарное, левое, правое",
        "code_error_desc": None
    },









    {
        "id": 23,
        "title": "Обходы бинарного дерева. (Прямой, обратный, концевой)",
        "theory": "Существует три основных рекурсивных способа обхода бинарного дерева:\n1. <b>Прямой (Preorder)</b>: Корень -> Левое поддерево -> Правое поддерево.\n2. <b>Обратный (Inorder)</b>: Левое поддерево -> Корень -> Правое поддерево. Для <b>бинарного дерева поиска</b> дает отсортированную последовательность.\n3. <b>Концевой (Postorder)</b>: Левое поддерево -> Правое поддерево -> Корень. Удобно для <b>удаления</b> дерева или вычисления выражений.",
        "code": "void inorder(node* cur) {\n    if (!cur) return;\n    inorder(cur->left);\n    printf(\"%d \", cur->val);\n    inorder(cur->right);\n}",
        "keywords": "Прямой, Обратный, Концевой, бинарного дерева поиска, удаления",
        "code_error_desc": "В рекурсивных функциях забывают базовый случай `if (!cur) return;`, что приводит к разыменованию NULL-указателя и падению программы (Segmentation fault)."
    },
    {
        "id": 24,
        "title": "Бинарное дерево поиска (BST). Свойства и вставка.",
        "theory": "<b>Бинарное дерево поиска (BST)</b> — дерево, в котором для каждой вершины выполняется свойство: все значения в левом поддереве <b>меньше</b> значения корня, а все в правом — <b>больше</b>. Это позволяет выполнять поиск, вставку и удаление в среднем за <b>O(log n)</b>.\nВставка нового элемента всегда происходит в <b>лист</b> (или пустое место), чтобы не нарушить структуру.",
        "code": "node* insert(node* root, int val) {\n    if (!root) {\n        node* new = malloc(sizeof(node));\n        new->val = val; new->left = new->right = NULL;\n        return new;\n    }\n    if (val < root->val) root->left = insert(root->left, val);\n    else if (val > root->val) root->right = insert(root->right, val);\n    return root;\n}",
        "keywords": "Бинарное дерево поиска, меньше, больше, O(log n), лист",
        "code_error_desc": "В условии вставки забывают `else if (val > root->val)`, оставляя просто `else`. Если в дереве уже есть такой же элемент, код создаст дубликат или зациклится."
    },
    {
        "id": 25,
        "title": "Удаление узла из бинарного дерева поиска.",
        "theory": "Удаление из BST рассматривает три случая:\n1. Удаляемый узел — <b>лист</b>: просто обнуляем ссылку на него.\n2. Узел имеет <b>одного потомка</b>: ссылка родителя перенаправляется на этого потомка.\n3. Узел имеет <b>двух потомков</b>: находим <b>минимум в правом поддереве</b> (или максимум в левом), копируем его значение в удаляемый узел, а затем рекурсивно удаляем этот минимум из правого поддерева.",
        "code": "node* find_min(node* n) {\n    while (n && n->left) n = n->left;\n    return n;\n}\nnode* delete(node* root, int val) {\n    if (!root) return root;\n    if (val < root->val) root->left = delete(root->left, val);\n    else if (val > root->val) root->right = delete(root->right, val);\n    else {\n        if (!root->left) { node* tmp = root->right; free(root); return tmp; }\n        if (!root->right) { node* tmp = root->left; free(root); return tmp; }\n        node* tmp = find_min(root->right);\n        root->val = tmp->val;\n        root->right = delete(root->right, tmp->val);\n    }\n    return root;\n}",
        "keywords": "лист, одного потомка, двух потомков, минимум в правом поддереве, free",
        "code_error_desc": "При удалении узла с двумя потомками студенты часто забывают рекурсивно удалить найденный минимум из правого поддерева. Это приводит к дублированию значения в дереве."
    },
    {
        "id": 26,
        "title": "Сбалансированные деревья. AVL-дерево. Определение.",
        "theory": "<b>AVL-дерево</b> — это самобалансирующееся бинарное дерево поиска, в котором для каждой вершины разница высот левого и правого поддеревьев (баланс-фактор) не превышает <b>1</b>.\nВысота AVL-дерева гарантированно остается <b>O(log n)</b>, что обеспечивает стабильное время операций. При нарушении баланса применяются <b>повороты</b> (ротации).",
        "code": "int height(node* n) {\n    return n ? n->h : 0;\n}\nint bfactor(node* n) {\n    return height(n->right) - height(n->left);\n}\nvoid fix_height(node* n) {\n    int hl = height(n->left), hr = height(n->right);\n    n->h = (hl > hr ? hl : hr) + 1;\n}",
        "keywords": "самобалансирующееся, 1, O(log n), повороты, баланс-фактор",
        "code_error_desc": "В функции вычисления высоты забывают обрабатывать случай `n == NULL`, возвращая вместо 0 разыменование NULL-указателя (`n->h`)."
    },
    {
        "id": 27,
        "title": "Вставка в AVL-дерево. Простые вращения (LL и RR).",
        "theory": "После вставки элемента в BST проверяем баланс-фактор от нового листа к корню. Если нарушается баланс:\n1. <b>LL-случай (правое вращение)</b>: перевес влево, и левый сын тоже перевешивает влево.\n2. <b>RR-случай (левое вращение)</b>: перевес вправо, и правый сын тоже перевешивает вправо.\nВращения локальны, выполняются за <b>O(1)</b> и восстанавливают баланс поддерева.",
        "code": "node* rot_right(node* q) {\n    node* p = q->left;\n    q->left = p->right;\n    p->right = q;\n    fix_height(q); fix_height(p);\n    return p;\n}\nnode* rot_left(node* p) {\n    node* q = p->right;\n    p->right = q->left;\n    q->left = p;\n    fix_height(p); fix_height(q);\n    return q;\n}",
        "keywords": "баланс-фактор, правое вращение, левое вращение, O(1), fix_height",
        "code_error_desc": "После вращения забывают обновить высоты узлов (`fix_height`). Сначала нужно обновить высоту у того узла, который оказался ниже, иначе расчет для верхнего узла будет неверным."
    },
    {
        "id": 28,
        "title": "Вращения в AVL (LR и RL). Балансировка.",
        "theory": "Если перевес идет \"внутрь\" (левый сын перевешивает вправо — <b>LR</b>, или правый влево — <b>RL</b>), простое вращение не поможет.\nРешение: выполняем <b>два вращения подряд</b>.\n1. LR: сначала <b>левое вращение</b> над левым сыном, затем <b>правое вращение</b> над корнем.\n2. RL: сначала <b>правое вращение</b> над правым сыном, затем <b>левое вращение</b> над корнем.",
        "code": "node* balance(node* p) {\n    fix_height(p);\n    if (bfactor(p) == 2) {\n        if (bfactor(p->right) < 0) p->right = rot_right(p->right);\n        return rot_left(p);\n    }\n    if (bfactor(p) == -2) {\n        if (bfactor(p->left) > 0) p->left = rot_left(p->left);\n        return rot_right(p);\n    }\n    return p;\n}",
        "keywords": "LR, RL, два вращения подряд, левое вращение, правое вращение",
        "code_error_desc": "В функции `balance` перепутан порядок проверок или не обновляется высота дочернего узла перед вторым вращением. Это приводит к некорректному выбору типа ротации."
    },
    {
        "id": 29,
        "title": "Куча (Heap). Свойства и представление в массиве.",
        "theory": "<b>Куча (Min/Max Heap)</b> — дерево, удовлетворяющее свойству кучи: значение в любом узле не меньше (или не больше) значений в его дочерних узлах. Куча всегда является <b>полным бинарным деревом</b>.\nЭффективно хранится в <b>массиве</b>. Для индекса `i`: левый сын `2*i+1`, правый `2*i+2`, родитель `(i-1)/2`. Вставка и извлечение работают за <b>O(log n)</b>.",
        "code": "int parent(int i) { return (i - 1) / 2; }\nint left(int i) { return 2 * i + 1; }\nint right(int i) { return 2 * i + 2; }\n// Пример MaxHeap: arr[parent(i)] >= arr[i]",
        "keywords": "полное бинарное дерево, массиве, 2*i+1, O(log n), MaxHeap",
        "code_error_desc": "При расчете индексов дочерних элементов забывают `+1` для левого сына или используют `2*i` вместо `2*i+1`, что соответствует нумерации с 1, а не с 0. Это ломает всю структуру кучи в C."
    },
    {
        "id": 30,
        "title": "Построение кучи (Heapify) и просеивание (SiftDown/SiftUp).",
        "theory": "Для восстановления свойства кучи после изменения элемента используются операции <b>просеивания вверх (SiftUp)</b> и <b>просеивания вниз (SiftDown)</b>.\n<b>Heapify</b> — линейный по времени (<b>O(n)</b>) алгоритм построения кучи из неупорядоченного массива. Запускает SiftDown от последних внутренних узлов к корню.",
        "code": "void sift_down(int* h, int i, int n) {\n    while (2*i + 1 < n) {\n        int left = 2*i + 1, right = 2*i + 2;\n        int largest = i;\n        if (left < n && h[left] > h[largest]) largest = left;\n        if (right < n && h[right] > h[largest]) largest = right;\n        if (largest == i) break;\n        int tmp = h[i]; h[i] = h[largest]; h[largest] = tmp;\n        i = largest;\n    }\n}",
        "keywords": "SiftDown, SiftUp, Heapify, O(n), просеивания вниз",
        "code_error_desc": "В цикле `while` условие `2*i + 1 < n` проверяет только левого сына. Забывают проверить границу массива для правого сына, что может привести к выходу за пределы массива."
    },
    {
        "id": 31,
        "title": "Очередь с приоритетами на куче.",
        "theory": "Очередь с приоритетами — абстрактный тип данных, где каждый элемент имеет <b>приоритет</b>. Извлекается всегда элемент с <b>наивысшим (или наименьшим) приоритетом</b>.\nРеализация на куче обеспечивает операции `insert` и `extract_max` за <b>O(log n)</b>, а peek вершины за <b>O(1)</b>. Широко используется в алгоритме Дейкстры и сортировке Хоара.",
        "code": "typedef struct {\n    int* data;\n    int size;\n    int capacity;\n} p_queue;\n\nvoid pq_push(p_queue* q, int val) {\n    if (q->size == q->capacity) return;\n    q->data[q->size] = val;\n    sift_up(q->data, q->size);\n    q->size++;\n}\nint pq_pop(p_queue* q) {\n    if (q->size == 0) return -1;\n    int res = q->data[0];\n    q->data[0] = q->data[--q->size];\n    sift_down(q->data, 0, q->size);\n    return res;\n}",
        "keywords": "приоритет, O(log n), O(1), sift_up, алгоритме Дейкстры",
        "code_error_desc": "При извлечении (`pq_pop`) заменяют корень последним элементом, уменьшают `size`, но забывают вызвать `sift_down`. В результате куча теряет свойство упорядоченности."
    },
    {
        "id": 32,
        "title": "Хеш-таблицы. Метод цепочек.",
        "theory": "Хеш-таблица отображает ключи в индексы массива с помощью <b>хеш-функции</b>. При <b>коллизиях</b> (разные ключи дают одинаковый хеш) используется <b>метод цепочек</b>: каждая ячейка массива хранит <b>связный список</b> элементов с одинаковым хешем.\nСреднее время поиска, вставки и удаления — <b>O(1)</b> при хорошем коэффициенте заполнения.",
        "code": "typedef struct entry {\n    char* key;\n    int val;\n    struct entry* next;\n} entry;\n\ntypedef struct {\n    entry** table;\n    int size;\n} hash_table;\n\nint hash(char* key, int m) {\n    int h = 0;\n    for (int i = 0; key[i]; i++) h = (h * 31 + key[i]) % m;\n    return h;\n}",
        "keywords": "хеш-функции, коллизиях, связный список, O(1), коэффициенте заполнения",
        "code_error_desc": "В функции хеширования забывают оператор `% m` в конце или внутри цикла. Это приводит к выходу индекса за границы массива таблицы (`segfault`)."
    },
    {
        "id": 33,
        "title": "Хеш-таблицы. Открытая адресация (линейное/квадратичное пробирование).",
        "theory": "При открытой адресации все элементы хранятся <b>непосредственно в массиве</b>. При коллизии ищется свободная ячейка по <b>пробной последовательности</b>.\n1. <b>Линейное пробирование</b>: `h(i) = (h0 + i) % m`. Страдает от <b>первичной кластеризации</b>.\n2. <b>Квадратичное пробирование</b>: `h(i) = (h0 + c1*i + c2*i^2) % m`. Уменьшает кластеризацию.\nКоэффициент заполнения не должен превышать <b>0.7</b>.",
        "code": "int find(int* table, int key, int m) {\n    int h0 = hash(key) % m;\n    for (int i = 0; i < m; i++) {\n        int idx = (h0 + i) % m;\n        if (table[idx] == EMPTY || table[idx] == key) return idx;\n    }\n    return -1;\n}",
        "keywords": "непосредственно в массиве, пробной последовательности, первичной кластеризации, 0.7",
        "code_error_desc": "В цикле поиска забывают условие `i < m`. Если таблица заполнена, цикл станет бесконечным. Также забывают обрабатывать удаленные элементы (помечать их как DELETED, а не EMPTY), иначе цепочка пробирования прервется."
    },
    {
        "id": 34,
        "title": "Графы. Матрица смежности и списки смежности.",
        "theory": "Граф G = (V, E) состоит из вершин и ребер.\n1. <b>Матрица смежности</b>: двумерный массив `A[N][N]`, где `A[i][j]=1`, если есть ребро. Память <b>O(V²)</b>. Быстрая проверка наличия ребра <b>O(1)</b>.\n2. <b>Списки смежности</b>: массив списков, где `adj[i]` содержит соседей вершины `i`. Память <b>O(V+E)</b>. Эффективно для <b>разреженных графов</b>.",
        "code": "// Матрица смежности\nint adj_mat[100][100];\n\n// Списки смежности\ntypedef struct node {\n    int v;\n    struct node* next;\n} node;\nnode* adj_list[100];\nvoid add_edge(int u, int v) {\n    node* n = malloc(sizeof(node));\n    n->v = v; n->next = adj_list[u]; adj_list[u] = n;\n}",
        "keywords": "O(V²), O(1), O(V+E), разреженных графов, malloc",
        "code_error_desc": "При добавлении ребра в список смежности забывают выделить память (`malloc`) или обнулить `next`. Также в неориентированном графе забывают добавить обратное ребро (`add_edge(v, u)`), из-за чего обход графа будет неполным."
    },
    {
        "id": 35,
        "title": "Обход графа в ширину (BFS) и в глубину (DFS).",
        "theory": "<b>DFS (Depth-First Search)</b>: идем вглубь, пока возможно, используя <b>стек</b> (или рекурсию). Память <b>O(V)</b>, время <b>O(V+E)</b>. Используется для поиска компонент связности, циклов.\n<b>BFS (Breadth-First Search)</b>: идем вширь, уровень за уровнем, используя <b>очередь</b>. Находит <b>кратчайший путь</b> в невзвешенном графе.",
        "code": "bool visited[100];\nvoid dfs(int v, node* adj[]) {\n    visited[v] = true;\n    for (node* cur = adj[v]; cur; cur = cur->next)\n        if (!visited[cur->v]) dfs(cur->v, adj);\n}\nvoid bfs(int start, node* adj[], int n) {\n    queue q; init_queue(&q);\n    q_push(&q, start); visited[start] = true;\n    while (!q_empty(&q)) {\n        int v = q_pop(&q);\n        for (node* cur = adj[v]; cur; cur = cur->next)\n            if (!visited[cur->v]) { visited[cur->v] = true; q_push(&q, cur->v); }\n    }\n}",
        "keywords": "стек, очередь, кратчайший путь, O(V+E), visited",
        "code_error_desc": "В BFS забывают отмечать вершину как посещенную (`visited[cur->v] = true`) сразу при добавлении в очередь, а не при извлечении. Это приводит к дублированию вершин в очереди и экспоненциальному росту времени работы."
    },









    {
        "id": 36,
        "title": "Алгоритм Дейкстры. Поиск кратчайшего пути во взвешенном графе.",
        "theory": "Алгоритм <b>Дейкстры</b> находит кратчайшие пути от одной вершины до всех остальных в графе с <b>неотрицательными весами</b>.\nИспользуется массив расстояний <b>d[]</b>, который инициализируется бесконечностью. На каждом шаге выбирается <b>непомеченная</b> вершина с минимальным расстоянием, и для её соседей выполняется <b>релаксация</b> (улучшение расстояния).\nВремя работы: <b>O(V²)</b> или <b>O(E log V)</b> с очередью с приоритетами.",
        "code": "void dijkstra(int graph[100][100], int n, int start) {\n    int d[100], visited[100];\n    for (int i = 0; i < n; i++) { d[i] = 1000000; visited[i] = 0; }\n    d[start] = 0;\n    for (int step = 0; step < n; step++) {\n        int v = -1;\n        for (int j = 0; j < n; j++)\n            if (!visited[j] && (v == -1 || d[j] < d[v])) v = j;\n        if (d[v] == 1000000) break;\n        visited[v] = 1;\n        for (int to = 0; to < n; to++) {\n            if (graph[v][to] > 0 && d[v] + graph[v][to] < d[to]) {\n                d[to] = d[v] + graph[v][to];\n            }\n        }\n    }\n}",
        "keywords": "Дейкстры, неотрицательными весами, d[], непомеченная, релаксация, O(V²)",
        "code_error_desc": "Студенты забывают проверять `graph[v][to] > 0` (или наличие ребра). Если в матрице смежности стоят нули там, где ребра нет, алгоритм ошибочно посчитает путь через несуществующее ребро с весом 0."
    },
    {
        "id": 37,
        "title": "Алгоритм Флойда-Уоршелла. Кратчайшие пути между всеми парами вершин.",
        "theory": "Алгоритм <b>Флойда-Уоршелла</b> находит кратчайшие расстояния между <b>всеми парами</b> вершин во взвешенном графе (может работать с отрицательными весами, но без отрицательных циклов).\nОснован на принципе <b>динамического программирования</b>. Мы перебираем промежуточные вершины <b>k</b> и проверяем, можно ли улучшить путь от <b>i</b> до <b>j</b> через <b>k</b>.\nВремя работы: <b>O(V³)</b>, требует <b>O(V²)</b> памяти.",
        "code": "void floyd(int d[100][100], int n) {\n    for (int k = 0; k < n; k++)\n        for (int i = 0; i < n; i++)\n            for (int j = 0; j < n; j++)\n                if (d[i][k] < 1000000 && d[k][j] < 1000000)\n                    d[i][j] = min(d[i][j], d[i][k] + d[k][j]);\n}",
        "keywords": "Флойда-Уоршелла, всеми парами, динамического программирования, промежуточные вершины, O(V³)",
        "code_error_desc": "Забывают проверку на бесконечность (`d[i][k] < INF`). Если её нет, при сложении двух бесконечностей может произойти <b>переполнение</b> (overflow) целого числа, и алгоритм найдет несуществующий путь."
    },
    {
        "id": 38,
        "title": "Остовные деревья. Алгоритм Прима.",
        "theory": "<b>Остовное дерево</b> — это подграф, который является деревом, содержит все вершины исходного графа и имеет минимально возможный <b>суммарный вес ребер</b>.\nАлгоритм <b>Прима</b> строит дерево постепенно: начинаем с одной вершины и на каждом шаге добавляем <b>ребро минимального веса</b>, которое соединяет уже построенное дерево с новой вершиной. Работает за <b>O(E log V)</b>.",
        "code": "void prim(int graph[100][100], int n) {\n    bool in_tree[100] = {false};\n    int min_edge[100];\n    for (int i = 0; i < n; i++) min_edge[i] = 1000000;\n    min_edge[0] = 0;\n    for (int step = 0; step < n; step++) {\n        int v = -1;\n        for (int j = 0; j < n; j++)\n            if (!in_tree[j] && (v == -1 || min_edge[j] < min_edge[v])) v = j;\n        in_tree[v] = true;\n        for (int to = 0; to < n; to++)\n            if (graph[v][to] > 0 && graph[v][to] < min_edge[to])\n                min_edge[to] = graph[v][to];\n    }\n}",
        "keywords": "Остовное дерево, суммарный вес ребер, Прима, минимального веса, O(E log V)",
        "code_error_desc": "Путают алгоритм Прима с Дейкстрой. В Приме мы обновляем `min_edge[to]` просто весом ребра `graph[v][to]`, а не суммой `d[v] + graph[v][to]`, как в Дейкстре."
    },
    {
        "id": 39,
        "title": "Алгоритм Крускала. Система непересекающихся множеств (DSU).",
        "theory": "Алгоритм <b>Крускала</b> находит минимальное остовное дерево. Он <b>сортирует все ребра</b> графа по весу и по очереди добавляет их в дерево, если они <b>не создают цикла</b>.\nДля проверки циклов используется структура <b>DSU (Disjoint Set Union)</b> — система непересекающихся множеств. Она позволяет быстро проверять, принадлежат ли две вершины одной компоненте связности, и объединять их.",
        "code": "int parent[100], rank[100];\nint find(int v) {\n    if (v == parent[v]) return v;\n    return parent[v] = find(parent[v]); // сжатие пути\n}\nvoid unite(int a, int b) {\n    a = find(a); b = find(b);\n    if (a != b) {\n        if (rank[a] < rank[b]) swap(a, b);\n        parent[b] = a;\n        if (rank[a] == rank[b]) rank[a]++;\n    }\n}",
        "keywords": "Крускала, сортирует все ребра, не создают цикла, DSU, компоненте связности",
        "code_error_desc": "В функции `find` забывают реализовать <b>сжатие пути</b> (`parent[v] = find(parent[v])`). Без этого при длинных цепочках DSU работает за O(N), а не почти за O(1)."
    },
    {
        "id": 40,
        "title": "Топологическая сортировка ориентированного графа.",
        "theory": "<b>Топологическая сортировка</b> — линейное упорядочивание вершин <b>ориентированного графа без циклов (DAG)</b>, при котором для каждого ребра (u, v) вершина u стоит <b>раньше</b> вершины v.\nРеализуется с помощью <b>DFS</b>. Вершина добавляется в результат <b>после</b> рекурсивного обхода всех её потомков (концевой обход), а затем итоговый массив <b>инвертируется</b>.",
        "code": "bool visited[100];\nint order[100], timer = 0;\nvoid dfs(int v) {\n    visited[v] = true;\n    for (int to : adj[v])\n        if (!visited[to]) dfs(to);\n    order[timer++] = v;\n}\nvoid top_sort(int n) {\n    for (int i = 0; i < n; i++) if (!visited[i]) dfs(i);\n    for (int i = 0; i < n / 2; i++) swap(order[i], order[n - 1 - i]);\n}",
        "keywords": "Топологическая сортировка, ориентированного графа без циклов, раньше, DFS, после, инвертируется",
        "code_error_desc": "Забывают, что граф должен быть <b>ацикличным</b> (DAG). Если в графе есть цикл, топологическая сортировка невозможна, а данный код просто выдаст некорректный результат без ошибки."
    },
    {
        "id": 41,
        "title": "Базовые сортировки: выбором, вставками, пузырьком.",
        "theory": "Три \"школьные\" сортировки с временем работы <b>O(n²)</b>:\n1. <b>Сортировка выбором</b>: ищем минимум в хвосте и меняем его с первым неотсортированным элементом.\n2. <b>Сортировка вставками</b>: берем элемент и \"вставляем\" его на правильное место в уже отсортированную часть (сдвигая элементы).\n3. <b>Пузырьковая сортировка</b>: последовательно сравниваем соседние элементы и меняем их местами, если они стоят в неправильном порядке.\nВсе они работают <b>на месте</b> (O(1) памяти) и являются <b>устойчивыми</b> (кроме выбора).",
        "code": "void selection_sort(int* arr, int n) {\n    for (int i = 0; i < n - 1; i++) {\n        int min_idx = i;\n        for (int j = i + 1; j < n; j++)\n            if (arr[j] < arr[min_idx]) min_idx = j;\n        swap(arr[i], arr[min_idx]);\n    }\n}",
        "keywords": "O(n²), Сортировка выбором, на месте, устойчивыми, min_idx",
        "code_error_desc": "В сортировке выбором внешний цикл идет до `n - 1`. Если запустить его до `n`, на последней итерации `min_idx` может выйти за границы или произойдет лишняя итерация."
    },
    {
        "id": 42,
        "title": "Сортировка слиянием (Merge Sort).",
        "theory": "<b>Merge Sort</b> — алгоритм сортировки, основанный на принципе <b>\"разделяй и властвуй\"</b>.\nМассив рекурсивно делится пополам до массивов размером 1. Затем отсортированные части <b>сливаются</b> в одну.\nВремя работы: гарантированные <b>O(n log n)</b>. Требует <b>O(n)</b> дополнительной памяти для буфера слияния. Является <b>устойчивой</b>.",
        "code": "void merge(int* arr, int l, int m, int r) {\n    int tmp[r - l + 1], k = 0;\n    int i = l, j = m + 1;\n    while (i <= m && j <= r) {\n        if (arr[i] <= arr[j]) tmp[k++] = arr[i++];\n        else tmp[k++] = arr[j++];\n    }\n    while (i <= m) tmp[k++] = arr[i++];\n    while (j <= r) tmp[k++] = arr[j++];\n    for (int p = 0; p < k; p++) arr[l + p] = tmp[p];\n}\nvoid merge_sort(int* arr, int l, int r) {\n    if (l >= r) return;\n    int m = l + (r - l) / 2;\n    merge_sort(arr, l, m);\n    merge_sort(arr, m + 1, r);\n    merge(arr, l, m, r);\n}",
        "keywords": "разделяй и властвуй, сливаются, O(n log n), O(n), устойчивой",
        "code_error_desc": "При вычислении середины `m = (l + r) / 2` может произойти <b>переполнение</b> целого числа, если `l + r` больше `INT_MAX`. Правильно писать `m = l + (r - l) / 2`."
    },
    {
        "id": 43,
        "title": "Быстрая сортировка Хоара (Quick Sort).",
        "theory": "<b>Quick Sort</b> — алгоритм <b>\"разделяй и властвуй\"</b>. Выбирается <b>опорный элемент (pivot)</b>, массив разбивается на три части: меньшие pivot, равные и большие. Затем рекурсивно сортируются части \"меньшие\" и \"большие\".\nСреднее время работы: <b>O(n log n)</b>, требует <b>O(log n)</b> памяти на стек рекурсии. <b>Неустойчива</b>. В худшем случае (уже отсортированный массив) деградирует до <b>O(n²)</b>.",
        "code": "int partition(int* arr, int l, int r) {\n    int pivot = arr[l + (r - l) / 2];\n    int i = l, j = r;\n    while (i <= j) {\n        while (arr[i] < pivot) i++;\n        while (arr[j] > pivot) j--;\n        if (i <= j) { swap(arr[i], arr[j]); i++; j--; }\n    }\n    return i;\n}\nvoid quick_sort(int* arr, int l, int r) {\n    if (l < r) {\n        int q = partition(arr, l, r);\n        quick_sort(arr, l, q - 1);\n        quick_sort(arr, q, r);\n    }\n}",
        "keywords": "опорный элемент, O(n log n), O(log n), Неустойчива, O(n²), partition",
        "code_error_desc": "В функции `partition` выбирают `pivot = arr[l]` (первый элемент). Если массив уже отсортирован, это приводит к худшему случаю <b>O(n²)</b>. Нужно выбирать средний элемент или случайный."
    },
    {
        "id": 44,
        "title": "Пирамидальная сортировка (Heap Sort).",
        "theory": "<b>Heap Sort</b> использует структуру данных <b>куча (Max-Heap)</b>.\n1. Исходный массив <b>строится в кучу</b> за <b>O(n)</b> (Heapify).\n2. Максимальный элемент (корень) меняется местами с последним элементом, размер кучи уменьшается на 1.\n3. Новый корень <b>просеивается вниз (SiftDown)</b>.\nВремя работы: <b>O(n log n)</b>, память <b>O(1)</b>. <b>Неустойчива</b>.",
        "code": "void sift_down(int* arr, int i, int n) {\n    while (1) {\n        int j = i;\n        if (2*i + 1 < n && arr[2*i + 1] > arr[j]) j = 2*i + 1;\n        if (2*i + 2 < n && arr[2*i + 2] > arr[j]) j = 2*i + 2;\n        if (i == j) break;\n        swap(arr[i], arr[j]);\n        i = j;\n    }\n}\nvoid heap_sort(int* arr, int n) {\n    for (int i = n / 2 - 1; i >= 0; i--) sift_down(arr, i, n);\n    for (int i = n - 1; i > 0; i--) {\n        swap(arr[0], arr[i]);\n        sift_down(arr, 0, i);\n    }\n}",
        "keywords": "куча, Max-Heap, O(n), просеивается вниз, O(n log n), O(1), Неустойчива",
        "code_error_desc": "При построении кучи цикл начинается с `i = n / 2 - 1`. Если начать с `n - 1`, мы будем вызывать sift_down для листьев, что бессмысленно и замедлит алгоритм."
    },
    {
        "id": 45,
        "title": "Сортировка подсчетом и поразрядная сортировка.",
        "theory": "1. <b>Сортировка подсчетом (Counting Sort)</b>: подсчитывает количество каждого уникального элемента, а затем записывает их в порядке возрастания. Время <b>O(n + k)</b>, где k — диапазон значений. <b>Устойчива</b>.\n2. <b>Поразрядная сортировка (Radix Sort)</b>: сортирует числа, обрабатывая их по одному разряду (от младшего к старшему), используя <b>устойчивую</b> сортировку подсчетом как базу. Время <b>O(d * (n + k))</b>.",
        "code": "void counting_sort(int* arr, int n, int max_val) {\n    int count[max_val + 1] = {0};\n    for (int i = 0; i < n; i++) count[arr[i]]++;\n    int idx = 0;\n    for (int i = 0; i <= max_val; i++)\n        while (count[i]-- > 0) arr[idx++] = i;\n}",
        "keywords": "подсчетом, O(n + k), Устойчива, Поразрядная, Radix Sort, d * (n + k)",
        "code_error_desc": "В сортировке подсчетом забывают инициализировать массив `count` нулями. В C локальные массивы содержат \"мусор\", что приведет к абсолютно неверным результатам."
    },
    {
        "id": 46,
        "title": "Линейный и бинарный поиск в массиве.",
        "theory": "1. <b>Линейный поиск</b>: перебирает все элементы по порядку. Время <b>O(n)</b>. Работает на любых массивах.\n2. <b>Бинарный поиск</b>: делит отрезок пополам на каждом шаге. Время <b>O(log n)</b>. Требует <b>отсортированного</b> массива.\nИтеративная реализация бинарного поиска использует два указателя: <b>left</b> и <b>right</b>.",
        "code": "int binary_search(int* arr, int n, int target) {\n    int left = 0, right = n - 1;\n    while (left <= right) {\n        int mid = left + (right - left) / 2;\n        if (arr[mid] == target) return mid;\n        if (arr[mid] < target) left = mid + 1;\n        else right = mid - 1;\n    }\n    return -1;\n}",
        "keywords": "Линейный, O(n), Бинарный, O(log n), отсортированного, left, right",
        "code_error_desc": "В условии цикла пишут `left < right` вместо `left <= right`. Из-за этого алгоритм может пропустить искомый элемент, если он находится на позиции, где `left == right`."
    },
    {
        "id": 47,
        "title": "Двудольные графы и паросочетания.",
        "theory": "<b>Двудольный граф</b> — граф, вершины которого можно разбить на два множества <b>A</b> и <b>B</b> так, что каждое ребро соединяет вершину из A с вершиной из B.\n<b>Паросочетание</b> — набор ребер, в котором <b>никакие два ребра не имеют общей вершины</b>. <b>Максимальное паросочетание</b> содержит наибольшее возможное количество ребер. Применяется в задачах назначения (например, работа и работник).",
        "code": None,
        "keywords": "Двудольный граф, A, B, Паросочетание, никакие два ребра, Максимальное паросочетание",
        "code_error_desc": None
    },
    {
        "id": 48,
        "title": "Алгоритм Куна для поиска максимального паросочетания.",
        "theory": "Алгоритм <b>Куна</b> находит максимальное паросочетание в двудольном графе за время <b>O(V * E)</b>.\nАлгоритм жадно перебирает вершины левой доли. Для каждой вершины пытается найти <b>увеличивающую цепь</b> с помощью <b>DFS</b>. Если цепь найдена, паросочетание увеличивается на одно ребро.",
        "code": "int match[100]; // match[v] = с какой вершиной из левой доли соединена v\nbool used[100];\nbool dfs(int v) {\n    for (int to : adj[v]) {\n        if (!used[to]) {\n            used[to] = true;\n            if (match[to] == -1 || dfs(match[to])) {\n                match[to] = v;\n                return true;\n            }\n        }\n    }\n    return false;\n}",
        "keywords": "Куна, O(V * E), увеличивающую цепь, DFS, match",
        "code_error_desc": "Забывают инициализировать массив `match` значениями `-1` перед запуском алгоритма. Если там будет \"мусор\", DFS попытается пойти по несуществующим вершинам и упадет."
    },
    {
        "id": 49,
        "title": "Динамическое программирование. Задача о черепашке.",
        "theory": "<b>Динамическое программирование (ДП)</b> — метод решения сложных задач путем разбиения их на <b>более простые подзадачи</b>.\nКлассическая задача: <b>Черепашка</b> должна добраться из левого верхнего угла таблицы в правый нижний, двигаясь только <b>вправо</b> и <b>вниз</b>, собирая максимум монет.\nРешение: `dp[i][j] = max(dp[i-1][j], dp[i][j-1]) + cost[i][j]`.",
        "code": "int dp[100][100];\nvoid turtle(int cost[100][100], int n, int m) {\n    dp[0][0] = cost[0][0];\n    for (int i = 1; i < n; i++) dp[i][0] = dp[i-1][0] + cost[i][0];\n    for (int j = 1; j < m; j++) dp[0][j] = dp[0][j-1] + cost[0][j];\n    for (int i = 1; i < n; i++)\n        for (int j = 1; j < m; j++)\n            dp[i][j] = max(dp[i-1][j], dp[i][j-1]) + cost[i][j];\n}",
        "keywords": "Динамическое программирование, подзадачи, Черепашка, вправо, вниз, max",
        "code_error_desc": "Забывают корректно инициализировать первую строку и первый столбец массива `dp`. Если просто запустить двойной цикл с `i=0, j=0`, произойдет обращение к `dp[-1][j]` или `dp[i][-1]`, что вызовет падение."
    },
    {
        "id": 50,
        "title": "Жадные алгоритмы. Задача о выборе заявок.",
        "theory": "<b>Жадный алгоритм</b> — алгоритм, принимающий <b>локально оптимальное</b> решение на каждом шаге.\nЗадача: есть заявки с временем начала и конца. Нужно выполнить <b>максимальное количество</b> заявок, не допуская их пересечения.\nРешение: сортируем заявки по <b>времени окончания</b> и жадно берем первую, которая не пересекается с последней выбранной.",
        "code": "typedef struct { int start, end; } task;\nint cmp(const void* a, const void* b) {\n    return ((task*)a)->end - ((task*)b)->end;\n}\nint max_tasks(task* arr, int n) {\n    qsort(arr, n, sizeof(task), cmp);\n    int count = 1, last_end = arr[0].end;\n    for (int i = 1; i < n; i++) {\n        if (arr[i].start >= last_end) {\n            count++;\n            last_end = arr[i].end;\n        }\n    }\n    return count;\n}",
        "keywords": "Жадные, локально оптимальное, максимальное количество, времени окончания, qsort",
        "code_error_desc": "Сортируют заявки по времени <b>начала</b>, а не <b>окончания</b>. Это классическая ошибка, которая приводит к тому, что жадный алгоритм перестает быть оптимальным и выдает неверный ответ."
    },







    
    {
        "id": 51,
        "title": "Абстрактные типы данных (АТД). Принципы сокрытия информации.",
        "theory": "<b>Абстрактный тип данных (АТД)</b> — это модель данных, которая определяет <b>тип</b>, его возможные значения, <b>операции</b> над ним и математические <b>аксиомы</b>, описывающие поведение операций.\nГлавный принцип АТД — <b>сокрытие реализации</b>. Пользователь видит только <b>интерфейс</b> (какие функции можно вызвать), но не знает, как данные хранятся в памяти (в массиве, в дереве или в списке).",
        "code": None,
        "keywords": "Абстрактный тип данных, тип, операции, аксиомы, сокрытие реализации, интерфейс",
        "code_error_desc": None
    },
    {
        "id": 52,
        "title": "Инкапсуляция в Си. Непрозрачные указатели (Opaque pointers).",
        "theory": "<b>Инкапсуляция</b> — объединение данных и методов в единое целое и скрытие внутренней реализации. В Си это реализуется через <b>непрозрачные указатели (opaque pointers)</b>.\nВ заголовочном файле (.h) объявляется только указатель на структуру, а само определение структуры находится в файле реализации (.c). Это защищает данные от прямого изменения извне.",
        "code": "// module.h\ntypedef struct MyStruct MyStruct;\nMyStruct* create();\nvoid destroy(MyStruct* obj);\n\n// module.c\nstruct MyStruct {\n    int hidden_data;\n};\nMyStruct* create() {\n    MyStruct* obj = malloc(sizeof(MyStruct));\n    obj->hidden_data = 0;\n    return obj;\n}",
        "keywords": "Инкапсуляция, непрозрачные указатели, opaque pointers, заголовочном файле, .h, .c",
        "code_error_desc": "Студенты часто забывают, что для работы с непрозрачным указателем в .c файле нужно обязательно подключить заголовочный файл, где определена сама структура, иначе компилятор выдаст ошибку 'incomplete type'."
    },
    {
        "id": 53,
        "title": "Наследование на Си. Встраивание структур (struct inclusion).",
        "theory": "<b>Наследование</b> в Си эмулируется путем <b>встраивания</b> базовой структуры в самое начало производной.\nБлагодаря тому, как устроена память в Си, указатель на производную структуру можно безопасно <b>привести</b> к указателю на базовую. Это позволяет использовать функции базового класса для работы с производным без написания нового кода.",
        "code": "typedef struct {\n    int x, y;\n} Point;\n\ntypedef struct {\n    Point base; // Базовая структура встраивается первой\n    int radius;\n} Circle;\n\nvoid move(Point* p, int dx, int dy) { p->x += dx; p->y += dy; }\n// Использование:\nCircle c = {{10, 20}, 5};\nmove((Point*)&c, 1, 1); // Работает как для Point!",
        "keywords": "Наследование, встраивания, начало производной, привести, базовую",
        "code_error_desc": "Если встроить базовую структуру не в самое начало производной (например, поставить её вторым полем), приведение указателей сломается, так как смещения полей в памяти не будут совпадать."
    },
    {
        "id": 54,
        "title": "Полиморфизм на Си. Таблицы виртуальных методов (vtable).",
        "theory": "<b>Полиморфизм</b> — возможность объектов с одинаковой спецификацией иметь различную реализацию. В Си он реализуется через <b>указатели на функции</b>.\nСоздается структура, содержащая указатели на функции (аналог <b>vtable</b> в C++). Разные 'объекты' инициализируют эти указатели своими конкретными реализациями, что позволяет вызывать методы 'вслепую'.",
        "code": "typedef struct {\n    void (*draw)(void* self);\n    void (*move)(void* self, int dx);\n} ShapeInterface;\n\ntypedef struct {\n    ShapeInterface interface;\n    int x, y;\n} Circle;\n\nvoid circle_draw(void* self) { printf(\"Drawing circle\\n\"); }\nvoid init_circle(Circle* c) {\n    c->interface.draw = circle_draw;\n}",
        "keywords": "Полиморфизм, указатели на функции, vtable, ShapeInterface, draw",
        "code_error_desc": "Забывают передавать `void* self` первым аргументом в функции-указатели. Без этого метод не будет иметь доступа к полям конкретного 'объекта' и не сможет с ним работать."
    },
    {
        "id": 55,
        "title": "Полиморфизм на Си. Бестиповые указатели (void*).",
        "theory": "Еще один способ реализации полиморфизма в Си — использование <b>бестиповых указателей (void*)</b>.\nФункция принимает `void*` и дополнительный параметр (например, `enum Type`), который указывает, к какому типу данных нужно привести указатель внутри функции. Это менее безопасно, чем таблицы методов, но очень гибко.",
        "code": "typedef enum { TYPE_INT, TYPE_FLOAT } DataType;\n\nvoid print_value(void* data, DataType type) {\n    if (type == TYPE_INT) printf(\"%d\\n\", *(int*)data);\n    else if (type == TYPE_FLOAT) printf(\"%f\\n\", *(float*)data);\n}",
        "keywords": "бестиповых указателей, void*, enum Type, привести, TYPE_INT",
        "code_error_desc": "Забывают явно приводить `void*` к нужному типу перед разыменованием (например, пишут `*data` вместо `*(int*)data`), что вызывает ошибку компиляции 'dereferencing void pointer'."
    },
    {
        "id": 56,
        "title": "Сложность алгоритмов. О-символика (Асимптотика).",
        "theory": "<b>Асимптотическая сложность</b> описывает, как растет время выполнения или потребление памяти алгоритма при увеличении размера входных данных <b>n</b>.\n<b>O-большое (O)</b> задает верхнюю границу (худший случай).\n<b>Ω (Омега)</b> задает нижнюю границу (лучший случай).\n<b>Θ (Тета)</b> задает точную границу (когда верхняя и нижняя совпадают).",
        "code": None,
        "keywords": "Асимптотическая сложность, n, O-большое, верхнюю границу, Ω, Θ",
        "code_error_desc": "Путают O и Θ. O(n) означает 'не хуже чем n', а Θ(n) означает 'ровно n'. В разговорной речи под 'О' часто подразумевают именно Θ."
    },
    {
        "id": 57,
        "title": "Классы сложности P, NP, NP-полные задачи.",
        "theory": "<b>Класс P</b> — задачи, которые решаются на машине Тьюринга за <b>полиномиальное время</b> (например, O(n^2)).\n<b>Класс NP</b> — задачи, решение которых можно <b>проверить</b> за полиномиальное время.\n<b>NP-полные задачи</b> — самые сложные задачи в NP. Если для одной из них найдут полиномиальный алгоритм, то P = NP. Примеры: <b>Задача коммивояжера</b>, <b>Выполнимость булевых формул (SAT)</b>.",
        "code": None,
        "keywords": "P, NP, полиномиальное время, проверить, NP-полные, P = NP, Задача коммивояжера",
        "code_error_desc": "Считают, что NP означает 'не полиномиальные'. На самом деле NP означает 'Nondeterministic Polynomial' (недетерминированно полиномиальные), то есть те, которые можно проверить быстро."
    },
    {
        "id": 58,
        "title": "Поиск подстроки. Алгоритм Кнута-Морриса-Пратта (КМП).",
        "theory": "Наивный поиск подстроки работает за <b>O(n*m)</b>. Алгоритм <b>КМП</b> использует <b>префикс-функцию</b>, чтобы избежать возвратов назад по тексту.\nПрефикс-функция для строки показывает длину наибольшего префикса, который одновременно является суффиксом. Это позволяет достичь времени <b>O(n + m)</b>.",
        "code": "void compute_lps(char* pat, int m, int* lps) {\n    int len = 0; lps[0] = 0;\n    int i = 1;\n    while (i < m) {\n        if (pat[i] == pat[len]) { len++; lps[i] = len; i++; }\n        else if (len != 0) len = lps[len - 1];\n        else { lps[i] = 0; i++; }\n    }\n}",
        "keywords": "O(n*m), КМП, префикс-функцию, возвратов назад, O(n + m), lps",
        "code_error_desc": "В цикле вычисления префикс-функции забывают ветку `else if (len != 0) len = lps[len - 1];`. Без этого алгоритм превращается в некорректный и не сможет правильно сдвигать шаблон."
    },
    {
        "id": 59,
        "title": "Префиксное дерево (Trie).",
        "theory": "<b>Trie (префиксное дерево)</b> — дерево, в котором каждый узел хранит один символ. Путь от корня до узла представляет собой <b>строку (префикс)</b>.\nПозволяет очень быстро искать строки, проверять наличие префикса и находить слова с общим началом. Время поиска зависит от <b>длины строки (L)</b>, а не от количества слов: <b>O(L)</b>.",
        "code": "typedef struct TrieNode {\n    struct TrieNode* children[26]; // для латиницы\n    bool is_end;\n} TrieNode;\n\nTrieNode* create_node() {\n    TrieNode* node = calloc(1, sizeof(TrieNode));\n    return node;\n}",
        "keywords": "Trie, префиксное дерево, один символ, префикс, O(L), calloc",
        "code_error_desc": "Забывают инициализировать массив `children` нулями (используют `malloc` вместо `calloc`). Это приводит к тому, что указатели на детей будут содержать 'мусор', и обход дерева упадет с Segmentation Fault."
    },
    {
        "id": 60,
        "title": "Задача о рюкзаке (0/1 Knapsack). Динамическое программирование.",
        "theory": "Дано <b>n</b> предметов с весом <b>w[i]</b> и стоимостью <b>v[i]</b>, и рюкзак вместимостью <b>W</b>. Нужно набрать предметов с <b>максимальной суммарной стоимостью</b>, не превысив вес.\nРешается методом <b>динамического программирования</b>.\n`dp[i][j]` — макс. стоимость, используя первые `i` предметов и вес не более `j`.\n`dp[i][j] = max(dp[i-1][j], dp[i-1][j-w[i]] + v[i])`.",
        "code": "int knapsack(int W, int w[], int v[], int n) {\n    int dp[n + 1][W + 1];\n    for (int i = 0; i <= n; i++) {\n        for (int j = 0; j <= W; j++) {\n            if (i == 0 || j == 0) dp[i][j] = 0;\n            else if (w[i - 1] <= j) dp[i][j] = max(dp[i - 1][j], dp[i - 1][j - w[i - 1]] + v[i - 1]);\n            else dp[i][j] = dp[i - 1][j];\n        }\n    }\n    return dp[n][W];\n}",
        "keywords": "n, w[i], v[i], W, максимальной суммарной стоимостью, динамического программирования, dp[i][j]",
        "code_error_desc": "Путают индексы в массивах `w` и `v` (забывают `-1`, так как `i` идет от 1 до n, а массивы с 0). Это приводит к выходу за границы массива или неверным расчетам."
    },
    {
        "id": 61,
        "title": "Наибольшая общая подпоследовательность (LCS).",
        "theory": "Даны две строки <b>A</b> и <b>B</b>. Нужно найти длину самой длинной <b>подпоследовательности</b>, которая встречается в обеих строках (символы могут идти не подряд, но порядок должен сохраняться).\nРешается <b>динамическим программированием</b>.\nЕсли `A[i] == B[j]`, то `dp[i][j] = dp[i-1][j-1] + 1`.\nИначе `dp[i][j] = max(dp[i-1][j], dp[i][j-1])`.",
        "code": "int lcs(char* A, char* B, int n, int m) {\n    int dp[n + 1][m + 1];\n    for (int i = 0; i <= n; i++) {\n        for (int j = 0; j <= m; j++) {\n            if (i == 0 || j == 0) dp[i][j] = 0;\n            else if (A[i - 1] == B[j - 1]) dp[i][j] = dp[i - 1][j - 1] + 1;\n            else dp[i][j] = max(dp[i - 1][j], dp[i][j - 1]);\n        }\n    }\n    return dp[n][m];\n}",
        "keywords": "A, B, подпоследовательности, не подряд, динамическим программированием, dp[i-1][j-1] + 1",
        "code_error_desc": "Путают подпоследовательность (LCS) с подстрокой. Для подстроки при несовпадении символов `dp[i][j]` обнуляется, а для подпоследовательности мы берем `max` от соседей."
    },
    {
        "id": 62,
        "title": "Быстрое возведение в степень (Бинарное).",
        "theory": "Наивное возведение в степень работает за <b>O(n)</b>. <b>Бинарное возведение в степень</b> (алгоритм быстрого возведения) использует идею: `a^n = (a^(n/2))^2`, если n четное, и `a * a^(n-1)`, если нечетное.\nЭто снижает сложность до <b>O(log n)</b>. Критически важно в криптографии.",
        "code": "long long binpow(long long a, long long n) {\n    long long res = 1;\n    while (n > 0) {\n        if (n % 2 == 1) res = res * a;\n        a = a * a;\n        n /= 2;\n    }\n    return res;\n}",
        "keywords": "O(n), Бинарное возведение, a^n = (a^(n/2))^2, O(log n), криптографии",
        "code_error_desc": "Забывают использовать тип `long long` или брать модуль на каждом шаге (`res = (res * a) % MOD`). При больших степенях происходит переполнение целого типа, и результат становится неверным."
    },
    {
        "id": 63,
        "title": "Алгоритм Евклида (НОД / GCD).",
        "theory": "<b>Наибольший общий делитель (НОД / GCD)</b> двух чисел можно найти с помощью <b>алгоритма Евклида</b>.\nОн основан на том, что `GCD(a, b) = GCD(b, a % b)`.\nВремя работы: <b>O(log(min(a, b)))</b>. Это один из самых древних и эффективных алгоритмов.",
        "code": "int gcd(int a, int b) {\n    while (b != 0) {\n        int temp = b;\n        b = a % b;\n        a = temp;\n    }\n    return a;\n}",
        "keywords": "Наибольший общий делитель, НОД, GCD, алгоритма Евклида, GCD(b, a % b), O(log(min(a, b)))",
        "code_error_desc": "В рекурсивной версии забывают базовый случай `if (b == 0) return a;`, что приводит к бесконечной рекурсии и переполнению стека."
    },
    {
        "id": 64,
        "title": "Реализация полиморфизма на Си с помощью адресного типа.",
        "theory": "<b>Полиморфизм</b> — многотиповость. В языке Си его можно реализовать с использованием <b>адресного типа</b> данных (бестиповый указатель <b>void*</b>).\nЭто позволяет создавать обобщенные функции, способные работать с различными типами данных, передавая дополнительно <b>метку типа</b> или размер элемента.",
        "code": "void print(void* ptr, char type) {\n    switch(type) {\n        case 'i': printf(\"%d\\n\", *((int*)ptr)); break;\n        case 'f': printf(\"%f\\n\", *((float*)ptr)); break;\n    }\n}",
        "keywords": "Полиморфизм, многотиповость, адресного типа, void*, метку типа",
        "code_error_desc": "Забывают правильно расставить скобки при приведении и разыменовании. Написание `*int*ptr` вызовет ошибку. Правильно: `*((int*)ptr)`."
    }




]
   